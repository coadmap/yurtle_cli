import Task from "data/task/taskEntity";
import { VFC } from "react";

type TaskInputListItemProps = {
  task: Task | null;
}

const TaskInputListItem: VFC<TaskInputListItemProps> = () => {
  return (

  )
}